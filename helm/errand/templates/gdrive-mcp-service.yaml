{{- if .Values.gdrive.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "errand.fullname" . }}-gdrive-mcp
  labels:
    {{- include "errand.labels" . | nindent 4 }}
    app.kubernetes.io/component: gdrive-mcp
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.gdrive.port }}
      targetPort: {{ .Values.gdrive.port }}
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/component: gdrive-mcp
{{- end }}
