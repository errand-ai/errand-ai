name: Build and Publish

on:
  push:
    branches: [main]
    paths-ignore:
      - 'CLAUDE.md'
      - 'openspec/**'
      - '.claude/**'
      - '.serena/**'
  pull_request:
    paths-ignore:
      - 'CLAUDE.md'
      - 'openspec/**'
      - '.claude/**'
      - '.serena/**'

env:
  REGISTRY: ghcr.io

jobs:
  version:
    runs-on: gha-runner
    outputs:
      tag: ${{ steps.version.outputs.tag }}
    steps:
      - uses: actions/checkout@v4

      - name: Read version
        id: version
        run: |
          VERSION=$(cat VERSION | tr -d '[:space:]')
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            TAG="${VERSION}-pr${{ github.event.pull_request.number }}"
          else
            TAG="${VERSION}"
          fi
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"

      - name: Check immutable version (main only)
        if: github.ref == 'refs/heads/main'
        run: |
          TAG="${{ steps.version.outputs.tag }}"
          for IMAGE in "ghcr.io/${{ github.repository_owner }}/errand" "ghcr.io/${{ github.repository_owner }}/errand-task-runner"; do
            if docker manifest inspect "${IMAGE}:${TAG}" > /dev/null 2>&1; then
              echo "::error::Image tag ${TAG} already exists for ${IMAGE}. Bump the VERSION file."
              exit 1
            fi
          done

  test:
    runs-on: gha-runner
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Run application tests
        working-directory: errand
        env:
          DATABASE_URL: "sqlite+aiosqlite://"
          PYTHONPATH: "."
        run: |
          pip install -r requirements.txt -r requirements-test.txt -q
          pytest tests/ -v

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
          registry-url: 'https://npm.pkg.github.com'

      - name: Run frontend tests
        working-directory: frontend
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          npm ci
          npm test

  build-errand:
    needs: [version, test]
    runs-on: gha-runner
    timeout-minutes: 10
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push errand image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/errand:${{ needs.version.outputs.tag }}
          build-args: APP_VERSION=${{ needs.version.outputs.tag }}
          secrets: |
            npm_token=${{ secrets.GITHUB_TOKEN }}

  build-task-runner:
    needs: [version, test]
    runs-on: gha-runner
    timeout-minutes: 10
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push task runner image
        uses: docker/build-push-action@v6
        with:
          context: ./task-runner
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/errand-task-runner:${{ needs.version.outputs.tag }}

  helm:
    needs: [version, build-errand, build-task-runner]
    runs-on: gha-runner
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4

      - name: Log in to container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Package and push Helm chart
        run: |
          TAG="${{ needs.version.outputs.tag }}"
          helm repo add valkey https://valkey.io/valkey-helm/
          helm dependency build helm/errand
          helm package helm/errand \
            --version "${TAG}" \
            --app-version "${TAG}"
          helm push "errand-${TAG}.tgz" "oci://${{ env.REGISTRY }}/${{ github.repository }}/charts"
