## Tasks

- [x] Handle `subscription_alert` in the WebSocket client's `_handle_message` — add a `subscription_alert` branch dispatched by `type` alongside the existing message types. On `payment_failed`: store alert in Settings table as `cloud_payment_warning` with `{ alert, plan, attempt_count, next_retry_at, final_attempt }`. On `payment_succeeded`: delete `cloud_payment_warning` from Settings table. Other message types (and non-JSON frames, rejected before dispatch) are unaffected.
- [x] Extend `GET /api/cloud/status` with `payment_warning` — include `payment_warning` in the `subscription` object when a `cloud_payment_warning` Setting exists, containing `{ alert, plan, attempt_count, next_retry_at, final_attempt }`. Omit when no Setting exists.
- [x] Update Cloud Service settings UI — on `/settings/cloud`, when `payment_warning` is present in the status response: show amber warning indicator next to subscription expiry line. If `final_attempt` is false: "Payment failed — retrying {formatted next_retry_at date}". If `final_attempt` is true: "Payment failed — subscription expired" with red indicator.
- [x] Forward `subscription_alert` to errand-desktop — when a `subscription_alert` is received, forward the payload via the existing push_event mechanism as a `push_event` on the `system` channel (only when that channel is subscribed).
- [x] Show toast notification on payment alert — when a `subscription_alert` event is received in the frontend via WebSocket/SSE: `payment_failed` shows warning toast, `payment_succeeded` shows success toast.
- [x] Tests — `_handle_message` dispatches `subscription_alert` messages correctly, `cloud_payment_warning` Setting stored/cleared, `GET /api/cloud/status` includes/omits `payment_warning`, non-alert messages (e.g. `webhook`) still dispatch and ACK as before.
