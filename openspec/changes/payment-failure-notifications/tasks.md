## Tasks

- [ ] Handle `subscription_alert` in WebSocket client pubsub loop — parse incoming messages as JSON, if `type` is `subscription_alert` dispatch to payment alert handling instead of webhook drain. On `payment_failed`: store alert in Settings table as `cloud_payment_warning` with `{ alert, plan, attempt_count, next_retry_at, final_attempt }`. On `payment_succeeded`: delete `cloud_payment_warning` from Settings table. Fall back to existing behaviour if not JSON or no `type` field.
- [ ] Extend `GET /api/cloud/status` with `payment_warning` — include `payment_warning` in the `subscription` object when a `cloud_payment_warning` Setting exists, containing `{ alert, plan, attempt_count, next_retry_at, final_attempt }`. Omit when no Setting exists.
- [ ] Update Cloud Service settings UI — on `/settings/cloud`, when `payment_warning` is present in the status response: show amber warning indicator next to subscription expiry line. If `final_attempt` is false: "Payment failed — retrying {formatted next_retry_at date}". If `final_attempt` is true: "Payment failed — subscription expired" with red indicator.
- [ ] Forward `subscription_alert` to errand-desktop — when a `subscription_alert` is received in the pubsub loop, forward the payload via the existing WebSocket event mechanism as a `push_event` on the `system` channel.
- [ ] Show toast notification on payment alert — when a `subscription_alert` event is received in the frontend via WebSocket/SSE: `payment_failed` shows warning toast, `payment_succeeded` shows success toast.
- [ ] Tests — pubsub handler dispatches `subscription_alert` messages correctly, `cloud_payment_warning` Setting stored/cleared, `GET /api/cloud/status` includes/omits `payment_warning`, non-JSON pubsub messages still trigger webhook drain.
